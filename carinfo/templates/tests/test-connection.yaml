apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "carinfo.fullname" . }}-test-connection"
  labels:
    {{- include "carinfo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "carinfo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
